# shellcheck shell=bash
# Aliases and shell functions.
#
# Intentionally empty. The inventory found no aliases or functions on any
# host, so there was nothing to preserve here — this file exists as the
# place to put them once they are actually used day to day, rather than
# a wishlist copied from someone else's dotfiles.
